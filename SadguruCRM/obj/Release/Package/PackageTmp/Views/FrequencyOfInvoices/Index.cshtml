@model IEnumerable<SadguruCRM.Models.FrequencyOfInvoice>

@{
    ViewBag.Title = "Frequencies of Invoices";
}

<h2>Frequencies of Invoices</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
        <tr>
            <th>
                @Html.DisplayName("Frequency Of Invoice")
            </th>
            <th>
                @Html.DisplayName("Duration Between 2 Invoices Years")
            </th>
            <th>
                @Html.DisplayName("Duration Between 2 Invoices Months")
            </th>
            <th>
                @Html.DisplayName("Duration Between 2 Invoices Days")
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order)
            </th>
            <th>
                @Html.DisplayName("Created By")
            </th>
            <th>
                @Html.DisplayName("Created On")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FrequencyOfInvoice1)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Duration_Between_2_Invoices_Year)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Duration_Between_2_Invoices_Month)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Duration_Between_2_Invoices_Day)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Order)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserLogin.UserName)
                </td>
                <td>
                    @Html.ValueFor(modelItem => item.CreatedDate, "{0:dd/MM/yyyy}")
                </td>
                <td>
                    @Html.ActionLink(" ", "Edit", new { id = item.FrequencyOfInvoiceID }, new { @class = "fa fa-edit" }) |
                    @Html.ActionLink(" ", "Details", new { id = item.FrequencyOfInvoiceID }, new { @class = "fa fa-info" }) |
                    @Html.ActionLink(" ", "Delete", new { id = item.FrequencyOfInvoiceID }, new { @class = "fa fa-trash" })
                </td>
            </tr>
        }
    </tbody>
</table>

<script>
    $(document).ready(function () {
        
        var table = $('.table').DataTable({
            "bPaginate": false,
            fixedHeader: true,
        });

        $('.table thead tr:first th').each( function () {
            var title = $('.table thead th').eq( $(this).index() ).text();
            $(this).html( '<input type="text" placeholder="Search '+title+'" class="column_search"/>' );
        } );
        $('.table thead tr:first th').on( 'keyup', ".column_search",function () {
            table
                .column( $(this).parent().index() )
                .search( this.value )
                .draw();
        });
        $(".dataTables_filter").css("display", "none");
    } );
        //table.columns( '.Status' ).search( 'Important' ).draw();
</script>
